# ===============================================================
# Configurações do Banco de Dados (H2 em memória para desenvolvimento rápido)
# ===============================================================
spring.datasource.url=jdbc:h2:mem:vempraarena;DB_CLOSE_DELAY=-1
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.h2.console.enabled=true

# Configurações do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===============================================================
# Configurações do Supabase (API & Storage)
# ===============================================================
supabase.url=https://pvatfkyvdgeuyzklozir.supabase.co
supabase.key=sb_publishable_f32Lap6aPylKDK_mzav6eQ_xVrpK7BH
supabase.bucket.name=documentos_promotores

# ===============================================================
# Configuração Absoluta de Limite de Upload (100MB)
# ===============================================================
# Para versões do Spring Boot 2.x e 3.x modernas
spring.servlet.multipart.max-file-size=100MB
spring.servlet.multipart.max-request-size=100MB

# Segurança extra (Garante o mapeamento em versões específicas)
spring.http.multipart.max-file-size=100MB
spring.http.multipart.max-request-size=100MB

# Configurações adicionais para garantir que o upload funcione corretamente
spring.servlet.multipart.enabled=true
spring.servlet.multipart.resolve-lazily=false

server.tomcat.max-swallow-size=-1
# Habilita o multipart normalmente
spring.servlet.multipart.enabled=true

# Define limites altos para os 3 PDFs passarem folgados pelo Tomcat
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB