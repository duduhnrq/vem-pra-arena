# ===============================================================
# Configurações do Banco de Dados (PostgreSQL via Variáveis de Ambiente)
# ===============================================================
spring.datasource.url=${DB_URL}
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver

# Configurações do JPA/Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

# Opcional: exibe comandos SQL no log (útil em dev)
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ===============================================================
# Configurações do Supabase (API & Storage)
# ===============================================================
supabase.url=https://pvatfkyvdgeuyzklozir.supabase.co
supabase.key=sb_publishable_f32Lap6aPylKDK_mzav6eQ_xVrpK7BH
supabase.bucket.name=documentos_promotores

# ===============================================================
# Configuração Absoluta de Limite de Upload (50MB)
# ===============================================================
# Para versões do Spring Boot 2.x e 3.x modernas
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# Segurança extra (Garante o mapeamento em versões específicas)
spring.http.multipart.max-file-size=50MB
spring.http.multipart.max-request-size=50MB